# Command Execution

Functions such as `system`, `popen`, `execve`, `WinExec`, `ShellExecute`, and `CreateProcess` can launch child processes. Risk increases when command strings are built dynamically or include shell interpreters.

