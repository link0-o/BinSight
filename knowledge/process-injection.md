# Process Injection

Process injection on Windows often uses combinations of `OpenProcess`, `VirtualAllocEx`, `WriteProcessMemory`, and `CreateRemoteThread`. A single import is capability evidence; multiple imports together are stronger evidence.

